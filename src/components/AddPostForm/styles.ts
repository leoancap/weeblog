import styled from 'styled-components';

export const Container = styled.div`
  background: rgba(80,80,80,0.05);
  width: 50em;
  max-width: 95%;
  border-radius: 0.5em;
  padding: 1em;
  margin: 0 auto;
`;
